function addNums(numOne, numTwo) {
    console.log(`Addition is ${numOne} and ${numTwo} is ${numOne + numTwo}`);
}

export function subNums(numOne, numTwo) {
    console.log(`Subtration is ${numOne} and ${numTwo} is ${numOne - numTwo}`);
}

export function mulNums(numOne, numTwo) {
    console.log(`Multiplaction is ${numOne} and ${numTwo} is ${numOne * numTwo}`);
}

export function divNums(numOne, numTwo) {
    console.log(`Division is ${numOne} and ${numTwo} is ${numOne / numTwo}`);
}

export default addNums
