import addNums, {subNums, mulNums, divNums} from "./modules.js";

const valOne = 10
const valTwo = 10

// Task 4: 
addNums(valOne, valTwo)

// Task 3:
subNums(valOne, valTwo)
mulNums(valOne, valTwo)
divNums(valOne, valTwo)